# PCF properties
# Replace the values below
#pcf.host = <ReplaceMe with PCF FQDN. Example: api.local.pcfdev.io>
#pcf.user = <ReplaceMe>
#pcf.password = <ReplaceMe>
#pcf.skipSslValidation = <ReplaceMe with either true|false>

# Firehose properties
# Valid firehose event types - { COUNTER_EVENT, VALUE_METRIC, CONTAINER_METRIC, ERROR, HTTP_START_STOP, LOG_MESSAGE }
#pcf.firehoseEventTypes = <Replace me with one or more values from above list. Example: COUNTER_EVENT, VALUE_METRIC>
# Choose a value between 1 and 8 inclusive. Default to 4
#pcf.firehoseParallelism = 4
#pcf.firehoseSubscriptionId = firehose-to-wavefront-proxy

# Wavefront-Proxy properties
wavefront.proxy.hostname = tcp.wavefront.io
wavefront.proxy.port = 1088

# Logging properties
logging.level.org.springframework.web=DEBUG

### =============================== ###
# Delete the lines below ...
# Local PCF instance
#pcf.host = api.local.pcfdev.io
#pcf.user = admin
#pcf.password = admin
#pcf.skipSslValidation = true

# AWS Wavefront PCF instance
pcf.host = api.sys.wavefront.io
pcf.user = svc_firehose
pcf.password = Testing123
pcf.skipSslValidation = true

# Valid firehose event types - { COUNTER_EVENT, VALUE_METRIC, CONTAINER_METRIC, ERROR, HTTP_START_STOP, LOG_MESSAGE }
pcf.firehose.eventTypes = COUNTER_EVENT, VALUE_METRIC
pcf.firehose.parallelism = 4
pcf.firehose.subscriptionId = firehose-to-wavefront-proxy

# End of delete lines ...
### =============================== ###